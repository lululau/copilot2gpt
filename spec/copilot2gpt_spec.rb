# frozen_string_literal: true

RSpec.describe Copilot2gpt do
  it "has a version number" do
    expect(Copilot2gpt::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
